const AppName = () => {

    // Variable to store the app name
    const appName = "Instant Nexus";

    return (
        <div className="text-4xl">
            {appName}
        </div>
    );
};

export default AppName;